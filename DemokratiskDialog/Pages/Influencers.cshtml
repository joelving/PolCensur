@page "/influencers"
@inject UsersToCheck influencers
@model DemokratiskDialog.Pages.InfluencersModel
@{
    ViewData["Title"] = "Hvem kontrollerer vi?";
}

<h1 class="display-4">@ViewData["Title"]</h1>

<p class="lead">Vi kontrollerer pt. @influencers.All.Count() forskellige indflydelsesrige Twitter-profiler. Nedenfor finder du en oversigt fordelt på forskellige kategorier.</p>

<style>
    .card {
        border: none;
    }
</style>

<div class="card-columns">
    @foreach (var group in influencers)
    {
    <div class="card">
        <h2>@group.Key.Description() (i alt @group.Value.Count())</h2>
        @foreach (var profile in group.Value.OrderBy(p => p.Name))
        {
        <div>@profile.Name <small>(<a href="https://twitter.com/@profile.ScreenName" title="Besøg @profile.Name's Twitter-profil" target="_blank" rel="noopener">@@@profile.ScreenName</a>)</small></div>
        }
    </div>
    }
</div>
